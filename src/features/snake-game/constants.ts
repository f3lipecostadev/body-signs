export const GRID_SIZE = 20;
// Fixed pixel fallback (used only where CSS clamp isn't available).
export const BOARD_PIXEL_SIZE = 680;
// Responsive size: scales with viewport, capped between 560px and 840px.
export const BOARD_SIZE_CSS = "clamp(560px, 74vh, 840px)";

export const INITIAL_SPEED = 220;
export const MIN_SPEED = 70;
export const SPEED_STEP = 10;

export const CORRECT_COUNTDOWN_SECONDS = 3;
export const INITIAL_SNAKE_SIZE = 3;